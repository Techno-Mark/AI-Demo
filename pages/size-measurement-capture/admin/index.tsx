import React from "react";
import Admin from "tsconfig.json/components/SizeMeasurment/Admin`";

const id = () => {
  return (
    <main className={`min-h-screen bg-white text-black`}>
      <Admin />
    </main>
  );
};

export default id;
